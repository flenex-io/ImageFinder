import React from 'react';
import AppBar from 'material-ui/AppBar';

const NavBar = () => (
  <AppBar title="Flx Image Finder" style={{backgroundColor: "#343541", height: "20%"}} />
);

export default NavBar;
